source 'https://github.com/CocoaPods/Specs.git'

platform :ios, "8.0"
inhibit_all_warnings!

target :Specs do
  pod 'Specta', '~> 1.0'
  pod 'Expecta', '~> 1.0'
  pod 'OCMock', '~> 3.0'
end

target :IntegrationSpecs do
  pod 'Specta', '~> 1.0'
  pod 'Expecta', '~> 1.0'
  pod 'OCMock', '~> 3.0'
end
